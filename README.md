## TODOs:

1. replace the playlist name with things that windows won't hate

2. add a `ytsync status` command to show some statistics or diffs about local and remote playlists

3. make the server automatically add the refresh and access tokens in the config file.

4. add `ytsync <PlaylistName>` to automatically sync a playlist by name without a prompt

5. add some **flags** like `--verbose` (to show all logs) or `-f` to force it download everything and ignore syncing

6. write a readme (lol) with a tutorial on how to setup/use the app.
